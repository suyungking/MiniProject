<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.kosa.mini.LoginDAO">
	<select id="getMember" resultType="org.kosa.mini.Member">
		select * from member where userid = #{userid}
	</select>
	
	<update id="setLoginTime" parameterType="String">
		update member set 
		   loginTime=now()
		where userid = #{userid}
	</update> 
	
	<update id="update">
		update member set 
			passwd=#{passwd},
			name=#{name},
			age = #{age}
		where userid= #{userid}	
	</update>
</mapper>